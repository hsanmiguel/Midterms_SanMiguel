# Midterms_SanMiguel

API versioning demo app built with React + TypeScript + Vite.

## Run locally

```bash
npm install
npm run dev
```

Open the local URL shown in the terminal (usually http://localhost:5173).

## Build

```bash
npm run build
```
